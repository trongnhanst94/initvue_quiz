import { View } from 'react-native';
import React from 'react';

import SM from '../SM';

class HeaderSM extends SM {
  // constructor(props) {
  //   super(props);
  // }
  render() {
    return (
      <View />
    );
  }
}

HeaderSM.propTypes = {
};

// how to use HeaderSM
//
// let optionHeader = {
// };
//
// <HeaderSM option={optionHeader} />

export default HeaderSM;
