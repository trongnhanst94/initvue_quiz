import { View } from 'react-native';
import React from 'react';

import SM from '../SM';

class DatePickerSM extends SM {
  render() {
    return (
      <View />
    );
  }
}

DatePickerSM.propTypes = {
};

// how to use DatePickerSM
//
// let optionDatePicker = {
// };
//
// <DatePickerSM option={optionDatePicker} />

export default DatePickerSM;
