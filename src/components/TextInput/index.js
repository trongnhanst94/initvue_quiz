import { View } from 'react-native';
import React from 'react';

import SM from '../SM';

class TextInputSM extends SM {
  // constructor(props) {
  //   super(props);
  // }

  render() {
    return (
      <View />
    );
  }
}

TextInputSM.propTypes = {
};

// how to use TextInputSM
//
// let optionTextInput = {
// };
//
// <TextInputSM option={optionTextInput} />
export default TextInputSM;
