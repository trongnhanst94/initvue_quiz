import { View } from 'react-native';
import React from 'react';

import SM from '../SM';

class MenuSM extends SM {
  // constructor(props) {
  //   super(props);
  // }

  render() {
    return (
      <View />
    );
  }
}

MenuSM.propTypes = {
};

// how to use MenuSM
//
// let optionMenu = {
// };
//
// <MenuSM option={optionMenu} />
export default MenuSM;
